<Page x:Class="SuitcaseRegisttry.Pages.Autorizaiton"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="540" d:DesignWidth="960"
      Title="Реестр чемоданов">

    <Grid Background="White" Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border BorderBrush="#D9D9D9" BorderThickness="1" Padding="12" CornerRadius="6">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Имя:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBox x:Name="txtLoginName" Width="180" Margin="0,0,10,0"/>

                <TextBlock Text="Роль:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox x:Name="cbRole" Width="150" Margin="0,0,10,0">
                    <ComboBoxItem Content="Passenger" IsSelected="True"/>
                    <ComboBoxItem Content="Inspector"/>
                    <ComboBoxItem Content="Logistician"/>
                </ComboBox>

                <Button Content="Войти" Width="110" Click="BtnLogin_Click" Background="#2F6FED" Foreground="White"/>
                <TextBlock x:Name="txtCurrentUser" Margin="18,0,0,0" VerticalAlignment="Center" Foreground="#4F4F4F" FontWeight="SemiBold"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="0,14,0,0">
            <Grid.Resources>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="#2F6FED"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Padding" Value="10,5"/>
                    <Setter Property="Margin" Value="0,6,6,0"/>
                </Style>
            </Grid.Resources>

            <StackPanel x:Name="PassengerPanel" Visibility="Collapsed">
                <TextBlock Text="Пассажир" FontSize="20" FontWeight="Bold" Margin="0,0,0,8"/>

                <WrapPanel Margin="0,0,0,8">
                    <TextBox x:Name="txtModel" Width="120" Margin="0,0,8,0" ToolTip="Model"/>
                    <TextBox x:Name="txtColour" Width="100" Margin="0,0,8,0" ToolTip="Colour"/>
                    <TextBox x:Name="txtWeight" Width="80" Margin="0,0,8,0" ToolTip="Weight"/>
                    <TextBox x:Name="txtDanger" Width="110" Margin="0,0,8,0" ToolTip="IDDegreeDanger"/>
                    <Button Content="Добавить чемодан" Click="BtnAddSuitcase_Click"/>
                    <Button Content="Отследить" Click="BtnTrackPassenger_Click"/>
                </WrapPanel>

                <DataGrid x:Name="dgPassenger" Height="260" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding IDSuitcase}" Width="55"/>
                        <DataGridTextColumn Header="QR" Binding="{Binding QRKod}" Width="*"/>
                        <DataGridTextColumn Header="Модель" Binding="{Binding Model}" Width="130"/>
                        <DataGridTextColumn Header="Статус" Binding="{Binding StatusName}" Width="150"/>
                        <DataGridTemplateColumn Header="Цвет" Width="70">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Width="16" Height="16" Background="{Binding StatusColor}" CornerRadius="8" HorizontalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
                <ListBox x:Name="lbPassengerTrack" Height="120" Margin="0,8,0,0"/>
            </StackPanel>

            <StackPanel x:Name="InspectorPanel" Visibility="Collapsed">
                <TextBlock Text="Инспектор" FontSize="20" FontWeight="Bold" Margin="0,0,0,8"/>
                <WrapPanel>
                    <Button Content="Досмотр" Click="BtnInspect_Click"/>
                    <TextBox x:Name="txtConfiscateSubject" Width="200" Margin="0,6,8,0" ToolTip="Что конфисковать"/>
                    <Button Content="Конфисковать" Click="BtnConfiscate_Click"/>
                </WrapPanel>
                <DataGrid x:Name="dgInspector" Margin="0,8,0,0" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding IDSuitcase}" Width="55"/>
                        <DataGridTextColumn Header="QR" Binding="{Binding QRKod}" Width="*"/>
                        <DataGridTextColumn Header="Опасность" Binding="{Binding IDDegreeDanger}" Width="90"/>
                        <DataGridTextColumn Header="Статус" Binding="{Binding StatusName}" Width="160"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>

            <StackPanel x:Name="LogisticPanel" Visibility="Collapsed">
                <TextBlock Text="Логист" FontSize="20" FontWeight="Bold" Margin="0,0,0,8"/>
                <WrapPanel>
                    <TextBox x:Name="txtSearchLost" Width="180" Margin="0,0,8,0" ToolTip="Поиск по QR"/>
                    <Button Content="Поиск" Click="BtnSearchLost_Click"/>
                    <TextBox x:Name="txtCoordinate" Width="220" Margin="0,6,8,0" ToolTip="Новая координата"/>
                    <Button Content="Обновить местоположение" Click="BtnUpdateTracking_Click"/>
                    <Button Content="Записать инцидент" Click="BtnAddIncident_Click"/>
                </WrapPanel>
                <DataGrid x:Name="dgLost" Margin="0,8,0,0" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding IDSuitcase}" Width="55"/>
                        <DataGridTextColumn Header="QR" Binding="{Binding QRKod}" Width="*"/>
                        <DataGridTextColumn Header="Владелец" Binding="{Binding Owner}" Width="90"/>
                        <DataGridTextColumn Header="Статус" Binding="{Binding StatusName}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
